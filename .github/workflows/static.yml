# GitHub Pages deployment disabled – site is deployed via Vercel instead.
# This file is intentionally left as a no-op to prevent CI errors.
# To re-enable GitHub Pages deployment in the future, restore the original workflow.

name: "(disabled) Deploy static content to Pages"

on:
  workflow_dispatch: # manual-only, never runs automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by Vercel. This workflow is disabled."
